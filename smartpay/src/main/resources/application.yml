spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/smartpay
    username: lotto
    password: 12345678
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

server:
  port: 8080
